starlette>=0.30.0
pydantic>=2
rich>=13
pytest>=8
torch>=2
numpy>=1
setuptools>=68,<82
aiohttp
# Note: do NOT add `substrate-interface` here. It installs the legacy `scalecodec`
# package, which shares a namespace with `cyscale` (used by
# `async-substrate-interface` >= 1.6). Having both triggers a
# `RuntimeError: Conflict detected: 'scalecodec' ... conflicts with 'cyscale'`
# at `btcli` startup. Use `from bittensor_wallet import Keypair` instead.
bittensor>=10.3.0
# async-substrate-interface is a transitive dep of bittensor; let bittensor pull the compatible version
bittensor-cli # Use latest or desired version
wandb
python-dotenv # Add dotenv for .env file handling
requests

# Phase 4: shared (validator + miner)
Pillow>=9.0.0

# Phase 4 miner-only packages (diffusion models, face validation, etc.)
# are in requirements-miner.txt.  Miners should also run:
#   pip install -r requirements-miner.txt