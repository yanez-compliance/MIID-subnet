starlette>=0.30.0
pydantic>=2
rich>=13
pytest>=8
torch>=2
numpy>=1
setuptools>=68,<82
aiohttp
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
# Note: do NOT add `substrate-interface` here. It installs the legacy `scalecodec`
# package, which shares a namespace with `cyscale` (used by
# `async-substrate-interface` >= 1.6, a transitive dependency of bittensor).
# Having both triggers a `RuntimeError: Conflict detected: 'scalecodec' ...
# conflicts with 'cyscale'` at `btcli` startup. Use `from bittensor.wallet
# import Keypair` instead (bundled into the `bittensor` package since v11 --
# the standalone `bittensor_wallet` / `bittensor-cli` packages are superseded
# and should NOT be installed alongside it).
#
# Pinned to the v11 line: v11 replaced the axon/dendrite/synapse networking
# stack (see MIID/compat/) and the whole bt.Config/argparse/bt.logging system,
# so this codebase will NOT work against bittensor <11 or >=12.
bittensor>=11,<12
cyscale  # required by async-substrate-interface >= 1.6 (transitive dep of bittensor)
wandb
python-dotenv # Add dotenv for .env file handling
requests

# Phase 4: shared (validator + miner)
Pillow>=9.0.0

# Phase 4 miner-only packages (diffusion models, face validation, etc.)
# are in requirements-miner.txt.  Miners should also run:
#   pip install -r requirements-miner.txt